+ Features
- Allow negative len (countdown from the end of the previous row, allow for successive row with negative row, ie. do not add).
- Display help

+ Code
- Definition structure (channels, device name, port - to know whether to merge message send and receive, typically mutes)

