+ Features
- Quit recording when moving around (instead of locking)
- Enable mouse to click the UI, would be more intuitive than the keyboard
- Song part?
- Display help

+ Format .trk
- Add a definition file (channels, device name, port - to know whether to merge message send and receive, typically mutes)
